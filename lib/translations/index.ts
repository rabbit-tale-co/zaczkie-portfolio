import { pl } from './pl'
import { en } from './en'
import { Language, TranslationData } from '../i18n'

export const translations: Record<Language, TranslationData> = {
  pl,
  en
}

export { pl, en }
